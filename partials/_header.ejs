<header>
  <nav class="navbar navbar-expand-md navbar-dark bg-dark">
    <a class="navbar-brand" href="/urls">TinyApp</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-item nav-link" href="/urls">My URLs</a>
        <a class="nav-item nav-link" href="/urls/new">Create New URL</a>
      </div>
    </div>
        <div class="form-group mb-2">
          <% if(user){ %>
          <form method="POST" action="/logout" class="form-inline">
          <span class="navbar-text" style="margin-right: 1em;">Logged in as: <%= user.email %></span>
          <button type="submit" class="btn btn-light"">Logout</button></form>
          <% } else { %>
            <a href="/login" class="text-light" style="margin-right: 1em;">Login</a><a href="/register" class="text-light">Register</a>
            </nav>
          <% } %>
        </div>
  </nav>
</header>